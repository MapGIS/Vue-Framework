import { Sprite } from "./sprite";
import {
  StoryType,
  StoryEventType,
  StoryEventOptions,
  Story,
  StoryBook,
  StoryEvent,
} from "./story";

import {
  Schemes,
  Interpolates,
  interpolateStepsPercent,
  schemeStepsPercent,
  scaleLinear,
  scaleOrdinal,
  scaleQuantize,
  scaleSequential,
} from "./color";

export {
  Sprite,
  StoryType,
  StoryEventType,
  StoryEventOptions,
  Story,
  StoryBook,
  StoryEvent,
  Schemes,
  Interpolates,
  interpolateStepsPercent,
  schemeStepsPercent,
  scaleLinear,
  scaleOrdinal,
  scaleQuantize,
  scaleSequential,
};

export default {
  Sprite,
  StoryType,
  StoryEventType,
  StoryEventOptions,
  Story,
  StoryBook,
  StoryEvent,
  Schemes,
  Interpolates,
  interpolateStepsPercent,
  schemeStepsPercent,
  scaleLinear,
  scaleOrdinal,
  scaleQuantize,
  scaleSequential,
};
